// todo

describe('Requests work with cookies passed from the client', () => {
  describe('It should return the Order Summary Json Schema when in checkout', () => {
    it('placeholder', () => {})
  })
})
